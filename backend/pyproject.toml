[tool.setuptools]
packages = ["etl"]

[project]
name = "etl"
requires-python = ">=3.9"
version = "0.0.1"

dependencies = [
  "fastapi",
  "uvicorn[standard]"
]

[project.optional-dependencies]
dev = ["pytest"]
test = ["pytest"]

[tool.pytest.ini_options]
addopts = [
    "--import-mode=importlib",
]